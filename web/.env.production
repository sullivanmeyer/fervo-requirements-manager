# Production API URL — set this to your Azure App Service URL.
# The React app's /api/* calls are proxied to this address by the
# Azure Static Web Apps routing config (staticwebapp.config.json).
# Leave empty if using the SWA proxy instead of a direct URL.
VITE_API_URL=https://your-app-service.azurewebsites.net
